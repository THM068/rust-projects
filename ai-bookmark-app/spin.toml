#:schema https://schemas.spinframework.dev/spin/manifest-v2/latest.json

spin_manifest_version = 2

[application]
name = "ai-bookmark-app"
version = "0.1.0"
authors = ["admin <thando.mafela@gmail.com>"]
description = "Ai bookmark app"

[[trigger.http]]
route = "/..."
component = "ai-bookmark-app"

[component.ai-bookmark-app]
source = "target/wasm32-wasip1/release/ai_bookmark_app.wasm"
allowed_outbound_hosts = []
[component.ai-bookmark-app.build]
command = "cargo build --target wasm32-wasip1 --release"
watch = ["src/**/*.rs", "Cargo.toml"]

[[trigger.http]]
route = "/static/..."
component = "fileserver"

[component.fileserver]
source = { url = "https://github.com/spinframework/spin-fileserver/releases/download/v0.3.0/spin_static_fs.wasm", digest = "sha256:ef88708817e107bf49985c7cefe4dd1f199bf26f6727819183d5c996baa3d148" }
files = [{ source = "assets", destination = "/" }]
